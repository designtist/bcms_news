	<h2>Aktuelles</h2>

	<% for article in @articles %>
	  <div class="news-item-browser">
	    <% if article.release_date -%>
	      <b><%= l article.release_date.to_date, :format => :long %></b>
	      <br/>
	    <% end %>
  
	    <%= link_to h(article.name), news_article_path(article.route_params) %>
	    <p class="small_margin"><%= article.summary %></p>
	  </div>
	<% end %>

	<% unless @portlet.more_link.blank? -%>
	  <!-- <div class="news-item-browser">
	    <% link_to "mehr", @portlet.more_link %>
	  </div> -->
	<% end %>

